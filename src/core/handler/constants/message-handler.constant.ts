export const enum MessageType {
    LOG,
    TEST,
    SUITE,
    DESCRIBE,
}

export const enum LogLevel {
    INFO,
    WARN,
    ERROR,
    DEBUG
}

export const enum ActionType {
    SKIP,
    TODO,
    FAILURE,
    SUCCESS,
    START
}
